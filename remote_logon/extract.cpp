#include<bits/stdc++.h>
using namespace std;
class AuditLogRow{
    private:
        string time;
vector<string> row;
public:
    AuditLogRow(vector<string> row){this->row = row;}

};
int main(int argc, char *argv[])
{
    // if(argc != 2)
    // {
    //     cout << "Usage: sudo ./extract" << " <filename>" << endl;
    //     return 1;
    // }
    // ifstream fin(argv[1]);
    string s="";
    for(int i=0;i<s.size();i++)
    {
        cout<<(int)s[i]<<" ";
    }
}